
export type attackMessageType={
    attackMessage:{
        type: string,
       data: string,
       id: number,
    }
    turnMessage?:{
       type: string,
       data: string,
       id: number,
   }
}